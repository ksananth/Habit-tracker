import 'package:flutter/material.dart';

abstract class AppColors {
  static const primary = Color(0xFF6C63FF);
  static const lime = Color(0xFFCDFF4E);
  static const limeLight = Color(0xFFF5FFD6);

  static const dark = Color(0xFF1A1A1A);
  static const muted = Color(0xFF4A4A4A);
  static const white = Color(0xFFFFFFFF);

  // Backgrounds
  static const splash = Color(0xFF121212);
  static const cardLight = Color(0xFFF5FFD6);

  // Button
  static const buttonPrimary = Color(0xFF1A1A1A);
  static const buttonText = Color(0xFFFFFFFF);
}